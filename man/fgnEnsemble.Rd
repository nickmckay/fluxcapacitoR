% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-varves.R
\name{fgnEnsemble}
\alias{fgnEnsemble}
\title{Simulate fractional Gaussian noise by circulant embedding}
\usage{
fgnEnsemble(n, n.ens, H)
}
\arguments{
\item{n}{Number of samples (years) per member.}

\item{n.ens}{Number of ensemble members (columns).}

\item{H}{Hurst parameter, \verb{0.5 < H < 1}.}
}
\value{
An \code{n} x \code{n.ens} matrix of unit-variance fGn.
}
\description{
Exact simulation of stationary fractional Gaussian noise (fGn, the
increments of fractional Brownian motion) with Hurst parameter \code{H}, using
the Davies-Harte / Wood-Chan circulant embedding. The embedding eigenvalues
are computed once and reused for every ensemble member -- all members are
drawn with a single column-wise FFT -- and the circulant is padded to a
power-of-2 length so the FFT is fast. For \verb{0.5 < H < 1} the embedding is
non-negative definite; tiny negative eigenvalues from round-off are clamped
to zero as a guard.
}
