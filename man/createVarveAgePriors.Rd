% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/varve-age-priors.R
\name{createVarveAgePriors}
\alias{createVarveAgePriors}
\title{Create varve-informed age-ensemble priors}
\usage{
createVarveAgePriors(
  DT,
  model.depths = NA,
  model.depth.step = 1,
  varveScalingFactor = 30,
  H = NULL,
  ar1 = NULL,
  kFrac = 1/3,
  n.ms.ens = 100,
  n.varve.ens = 100,
  heuristicOutlierRemoval = TRUE,
  outlierRemovedFraction = 0.95,
  progress = TRUE
)
}
\arguments{
\item{DT}{A LiPD distribution table: a list of distribution table objects,
each with \code{age}, \code{probabilityDensity}, and \code{depth} components.}

\item{model.depths}{Depths at which to evaluate the age model. If \code{NA}
(default), a regular sequence from 0 to the deepest dated sample in steps
of \code{model.depth.step}.}

\item{model.depth.step}{Depth increment used to construct \code{model.depths}
when they're not supplied.}

\item{varveScalingFactor}{Prior on the ratio of mean varve thickness to the
mean spline-derived sedimentation rate.}

\item{H}{Prior Hurst parameter for long-memory varve simulation. Specify
\code{H} or \code{ar1}, not both.}

\item{ar1}{Prior AR(1) coefficient for varve simulation.}

\item{kFrac}{Fraction of the number of dates to use as the spline basis
dimension \code{k} (bounded between 5 and the number of dates).}

\item{n.ms.ens}{Number of monotone-spline ensemble members.}

\item{n.varve.ens}{Number of varve ensemble members (currently must equal
\code{n.ms.ens}).}

\item{heuristicOutlierRemoval}{Apply \code{\link[=removeOutliers]{removeOutliers()}} to sampled ages?}

\item{outlierRemovedFraction}{Fraction of ensemble members that get the
outlier-removal treatment.}

\item{progress}{Show progress bars and messages?}
}
\value{
A list with \code{agePriors} (matrix of ensemble age priors),
\code{ageDepths} (the depths they're evaluated at), and \code{varvedPriorLogObj}
(log-likelihood of each ensemble member given the dated distributions).
}
\description{
Builds an ensemble of age-depth priors from a LiPD distribution table by
combining (1) low-frequency structure, from monotone spline fits through
ages sampled from the calibrated date distributions, with (2)
high-frequency structure, from simulated annual-layer (varve) thickness
variability with AR(1) or long-memory persistence.
}
