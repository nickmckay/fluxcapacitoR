% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/varve-age-priors.R
\name{addVarves}
\alias{addVarves}
\title{Add high-frequency varve variability to age-depth ensembles}
\usage{
addVarves(
  ages,
  model.depths,
  yrPerDepth,
  totalDepth,
  varveMean,
  H,
  ar1,
  n.varve.ens,
  DT,
  progress = TRUE
)
}
\arguments{
\item{ages}{Matrix of low-frequency age estimates (depth x ensemble member).}

\item{model.depths}{Depths corresponding to the rows of \code{ages} /
\code{yrPerDepth}.}

\item{yrPerDepth}{Matrix of years-per-depth (inverse sedimentation rate),
same shape as \code{ages}.}

\item{totalDepth}{Total depth spanned by the record, used to rescale
simulated varve depths.}

\item{varveMean}{Mean simulated varve thickness.}

\item{H}{Hurst parameter for long-memory varve simulation. Specify \code{H} or
\code{ar1}, not both.}

\item{ar1}{AR(1) coefficient for varve simulation.}

\item{n.varve.ens}{Number of varve ensemble members (must match the number
of columns of \code{ages}).}

\item{DT}{A LiPD distribution table (list of distribution table objects)
used to evaluate the fit.}

\item{progress}{Show progress bars?}
}
\value{
A list with \code{agePriors} (matrix of updated age priors), \code{ageDepths}
(depths they're evaluated at), and \code{varvedPriorLogObj} (log-likelihood of
each ensemble member).
}
\description{
The core of the flux model: takes an ensemble of low-frequency age-depth
estimates and a per-member sedimentation-rate (years-per-depth) estimate,
simulates annual-layer (varve) thickness variability with the requested
persistence, scales it to be consistent with total depth and total age, and
returns an updated ensemble of age priors plus their log-likelihood against
the dated distributions. Called repeatedly inside the parameter-fitting loop
(see the package vignette).
}
